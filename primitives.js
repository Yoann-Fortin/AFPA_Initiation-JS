// Les entiers, réels et flottants sont regroupés sous une seule primitive.
const int = 1;
const int2 = 1.5;

// Les chaînes de caractères
const string = 'A';
const string2 = "abc";
const string3 = `aBc`;

// Les fameux booléens
const bool = true;
const bool2 = false;

// Une primitive qui instancie une variable ou constante à null
const nullVariable = null;

// Primitive qui instancie à undefined, à éviter.
const undefinedVariable = undefined;

// Il existe d'autres primitives, moins utilisées, je vous laisse chercher.